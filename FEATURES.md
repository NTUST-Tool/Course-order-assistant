# 台科大選課助理 - 新功能說明

## 功能概覽

本程式現在提供兩大功能：

### 1. 原始選課分析功能
- 分析 HTML 檔案中的選課志願序
- 計算選課成功機率
- 提供課程資訊表格

### 2. 課程監測功能 ⭐ **新功能**
- 即時監測指定課程的選課人數
- 自動偵測課程空缺並提醒
- 支援多課程同時監測

## 使用方式

### 啟動程式

```bash
# 直接執行（會顯示主選單）
./Course-order-assistant

# 或直接指定檔案進行分析
./Course-order-assistant path/to/file.html
```

### 主選單

程式啟動後會顯示主選單：

```
========================================
      台科大選課助理 - 主選單
========================================
1. 執行原始選課分析功能
2. 執行課程監測功能
0. 結束程式
========================================
```

## 課程監測功能詳細說明

### 基本使用流程

1. **選擇功能 2** - 執行課程監測功能
2. **輸入課程代碼** - 可輸入多個，用逗號分隔
   - 範例：`CS1001301,EE2003501`
   - 範例（有空格也可）：`CS1001301, EE2003501`
3. **設定查詢間隔** - 預設 5 秒，可自訂
4. **開始監測** - 程式會持續查詢並顯示結果

### 課程代碼格式

- **長度**: 9 位字元
- **格式**: 例如 `CS1001301`
- **驗證**: 程式會自動驗證格式，無效代碼會被拒絕

### 監測輸出範例

```
[第 1 次查詢] 2025-12-22 14:30:15
--------------------------------------------------------------------------------
CS1001301 | 計算機概論 | 選課人數: 45/50 | ✅ 有空缺
EE2003501 | 電路學 | 選課人數: 60/60 | ❌ 已滿

等待 5.2 秒後進行下次查詢...
```

### 空缺提醒功能

當偵測到課程有空缺時，會：

1. **系統嗶聲** 🔔 - 連續三聲
2. **視覺提醒** - 顯示醒目的提示框：

```
\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\
🔔 課程有空缺！ 🔔
課程代碼: CS1001301
//////////////////////////////////////////////////
```

## 進階功能

### 1. 代碼校驗
- 輸入錯誤的代碼會顯示「代碼無效」
- 不會造成程式崩潰

### 2. 網路容錯
- 遇到網路問題會自動重試
- 最多重試 3 次
- 使用指數退避策略（2秒、4秒、8秒）

### 3. 輸入格式寬容度
- `A01, B02`（有空格）✅
- `A01,B02`（無空格）✅
- 自動轉換為大寫

### 4. 隨機延遲 (Jitter)
- 實際查詢間隔 = 設定值 ± 2 秒
- 模擬真人行為，避免被偵測

### 5. 自定義查詢間隔
- 可設定 1-3600 秒（1小時）
- 適合不同監測需求

### 6. 啟動狀態記憶
- 自動儲存上次查詢的課程清單
- 下次啟動時可選擇沿用或重新輸入
- 儲存於 `last_courses.txt`

## 錯誤處理

### 常見錯誤訊息

| 錯誤訊息 | 說明 | 解決方式 |
|---------|------|---------|
| `❌ 代碼無效或查詢失敗` | 課程代碼不存在或格式錯誤 | 檢查課程代碼是否正確 |
| `❌ 以下課程代碼格式無效` | 輸入的代碼格式不符合規則 | 確認代碼為 9 位字元 |
| `⚠️ 查詢失敗，X秒後重試` | 網路問題 | 等待自動重試 |
| `❌ 無法取得學期資訊` | 連線台科大系統失敗 | 檢查網路連線 |

## 操作提示

### 結束程式
- 按 `Ctrl+C` 結束監測
- 或在主選單選擇 `0`

### 最佳實踐
1. **合理設定間隔** - 建議 5-10 秒，避免過於頻繁
2. **穩定網路環境** - 確保網路連線穩定
3. **保持程式開啟** - 監測期間不要關閉視窗

## 技術特點

- ✅ 非同步架構，高效能
- ✅ 自動重試機制，容錯性強
- ✅ 隨機延遲，模擬真人
- ✅ 跨平台相容（Windows/macOS/Linux）
- ✅ 友善的使用者介面

## 注意事項

1. 程式會持續執行直到使用者手動結束
2. 監測過程中會產生網路流量
3. 建議在選課期間使用，以便及時掌握空缺
4. 系統嗶聲需要音效支援

## 範例使用場景

### 場景一：搶熱門課程
```
1. 啟動程式選擇監測功能
2. 輸入熱門課程代碼：CS1001301
3. 設定 5 秒間隔
4. 等待空缺提醒
5. 收到提醒後立即登入選課系統
```

### 場景二：監測多門課程
```
1. 輸入多個代碼：CS1001301,EE2003501,MA1005301
2. 設定 10 秒間隔（3門課程可設久一點）
3. 觀察各課程狀態
4. 發現空缺後優先選課
```

## 問題回報

如有任何問題或建議，請聯繫開發團隊。

---

**版本**: 1.3.0+
**更新日期**: 2025-12-22
