# 台科大選課助理 - 更新說明 (v1.4.0)

## 🎉 新增功能

### 1. ⌨️ Q 鍵快速退出監測
- **功能**：在監測過程中，隨時按下 `Q` 鍵即可結束監測
- **優點**：無需強制關閉程式，優雅地返回主選單
- **使用方式**：
  ```
  倒數計時: 4.3 秒 | 按 Q 結束監測
  ↑ 此時按 Q 即可立即停止
  ```

### 2. ⏱️ 實時倒數計時器
- **功能**：等待下次查詢時顯示精確倒數計時
- **更新頻率**：每 0.1 秒更新一次
- **顯示格式**：
  ```
  倒數計時: 4.7 秒 | 按 Q 結束監測
  倒數計時: 4.6 秒 | 按 Q 結束監測
  倒數計時: 4.5 秒 | 按 Q 結束監測
  ...
  ```
- **互動性**：實時提醒使用者可按 Q 退出

### 3. 🕐 智能時間單位解析
- **功能**：支援多種時間單位輸入
- **支援格式**：

| 輸入格式 | 說明 | 轉換結果 |
|---------|------|---------|
| `5` | 純數字 = 秒數 | 5 秒 |
| `3h` | 小時 | 10800 秒 (3×3600) |
| `10m` | 分鐘 | 600 秒 (10×60) |
| `1h30m` | 小時+分鐘 | 5400 秒 (3600+1800) |
| `2h30m50` | 小時+分鐘+秒 | 9050 秒 (7200+1800+50) |

- **使用範例**：
  ```
  請輸入查詢間隔（預設 5 秒，按 Enter 使用預設值）
  支援格式: 數字(秒), 3h(小時), 10m(分鐘), 1h30m(組合): 1h
  ✓ 已設定查詢間隔: 3600 秒
  ```

### 4. 🎯 增強視覺提醒
- **功能**：有空缺時的提醒更加醒目
- **改進**：反斜線和斜線各顯示 **三行**
- **顯示效果**：
  ```
  \\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\
  \\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\
  \\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\
  🔔 課程有空缺！ 🔔
  課程代碼: CS1001301
  //////////////////////////////////////////////////
  //////////////////////////////////////////////////
  //////////////////////////////////////////////////
  ```
- **音效**：同時發出三聲系統嗶聲

### 5. 🔄 主選單循環模式
- **功能**：執行完任何功能後自動返回主選單
- **適用場景**：
  1. 原始選課分析完成 → 按 Enter → 返回主選單
  2. 監測功能按 Q 退出 → 返回主選單
  3. 監測功能輸入錯誤選擇取消 → 返回主選單
  
- **使用流程**：
  ```
  主選單 → 選功能 1 → 分析完成 → 按 Enter → 主選單
         ↓
         選功能 2 → 監測中 → 按 Q → 主選單
         ↓
         選功能 0 → 結束程式
  ```

## 📋 功能對照表

| 功能 | 舊版本 | 新版本 |
|-----|--------|--------|
| **退出監測** | Ctrl+C (強制結束程式) | Q 鍵 (優雅返回主選單) |
| **等待顯示** | 靜態訊息 | 實時倒數計時 (0.1秒更新) |
| **時間輸入** | 僅支援秒數 | 支援 h/m 單位組合 |
| **空缺提醒** | 1 行框線 | 3 行框線 + 3 聲嗶聲 |
| **程式結束** | 直接退出 | 返回主選單繼續使用 |

## 🎮 使用範例

### 範例 1：快速監測並退出
```bash
# 啟動程式
./Course-order-assistant

# 選擇功能 2
請選擇功能 (0-2): 2

# 輸入課程代碼
請輸入要監測的課程代碼: CS1001301

# 設定 30 秒間隔
請輸入查詢間隔: 30

# 監測中...發現不需要了
倒數計時: 25.3 秒 | 按 Q 結束監測
按下 Q 鍵 ← 這裡按 Q

# 返回主選單
✓ 已停止監測，返回主選單...
請選擇功能 (0-2): 0
👋 再見！
```

### 範例 2：使用時間單位設定長時間監測
```bash
# 選擇監測功能
請選擇功能 (0-2): 2

# 輸入課程代碼
請輸入要監測的課程代碼: CS1001301,EE2003501

# 設定每 2 小時查詢一次
請輸入查詢間隔: 2h
✓ 已設定查詢間隔: 7200 秒

# 開始長時間監測...
# 可隨時按 Q 退出
```

### 範例 3：連續使用不同功能
```bash
# 第一次：分析選課
請選擇功能 (0-2): 1
請輸入 HTML 檔案路徑: courses.html
[顯示分析結果...]
按 Enter 鍵返回主選單...

# 第二次：監測課程
請選擇功能 (0-2): 2
[進行監測...]
按 Q 返回主選單

# 第三次：再次分析
請選擇功能 (0-2): 1
[再次分析...]
```

## ⚙️ 技術改進

### 1. 異步鍵盤監聽
- 使用 `tokio::io::AsyncReadExt` 實現非阻塞鍵盤輸入
- 使用 `Arc<AtomicBool>` 實現線程安全的退出信號

### 2. 時間解析算法
```rust
// 支援複雜的時間組合
"2h30m50" → 2×3600 + 30×60 + 50 = 9050 秒
"1h" → 1×3600 = 3600 秒
"30m" → 30×60 = 1800 秒
```

### 3. 主選單循環架構
```rust
loop {
    match show_main_menu() {
        0 => exit(0),           // 退出
        1 => analyze().await,    // 分析後返回
        2 => monitor().await,    // 監測後返回
    }
}
```

## 🔧 系統需求

- **Rust 版本**：1.70+
- **新增依賴**：
  - `tokio` (已有，使用 async I/O 功能)
  - `std::sync::atomic` (標準庫)
  
## 📝 注意事項

1. **Q 鍵響應**：
   - 僅在倒數計時期間有效
   - 查詢課程時不可中斷（因為正在網路請求）
   
2. **時間單位**：
   - 最大支援到 24 小時 (86400 秒)
   - 組合輸入時順序不限：`30m1h` 和 `1h30m` 效果相同
   
3. **倒數計時精度**：
   - 顯示精度：0.1 秒
   - 實際睡眠精度：毫秒級
   
4. **返回主選單**：
   - 所有錯誤情況都會返回主選單
   - 不會意外退出程式

## 🚀 升級建議

從舊版升級後：
1. ✅ 所有舊功能保持不變
2. ✅ 可以使用新的 Q 鍵退出
3. ✅ 時間輸入仍支援純數字
4. ✅ 無需修改使用習慣

## 📞 回饋與支援

如遇到問題或有改進建議，請提供：
- 使用的功能 (分析/監測)
- 輸入的時間格式
- 錯誤訊息截圖

---

**版本**: 1.4.0  
**更新日期**: 2025-12-22  
**主要貢獻**: 優化使用者體驗，提升互動性
